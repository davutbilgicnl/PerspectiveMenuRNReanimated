import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const PerspectiveMenu = () => {
  return (
    <View>
      <Text>Perspective Menu</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default PerspectiveMenu;
